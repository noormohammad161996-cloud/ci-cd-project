version: 2.1

jobs:
  placeholder:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout

      - run:
          name: Project Information
          command: |
            echo "===================================="
            echo " DevOps CI/CD Project"
            echo "===================================="
            echo "This project uses Jenkins for CI/CD."
            echo "CircleCI configuration is included"
            echo "only as a reference."
            echo "===================================="

workflows:
  version: 2

  placeholder-workflow:
    jobs:
      - placeholder